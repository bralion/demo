# promise
1.获取代码

复制下列代码在  gitbash上运行便可以复制demo相关代码

mkdir promise;cd promise;git init;git remote add -f origin git@github.com:bralion/demo.git;git config core.sparsecheckout true;echo "severAndClient/promise/" >> .git/info/sparse-checkout;git checkout master;


mkdir promise;
cd promise;
git init;     							   #新建一个空的库
git remote add -f origin git@github.com:bralion/demo.git;          #远程加载库
git config core.sparsecheckout true;          			   #允许使用sparse checkout
echo "severAndClient/promise/" >> .git/info/sparse-checkout;         #将需要下载的文件路径加入到配置文件，需要添加多个则多写一条记录
git checkout master;						   #拉取相关的文件

2.启动项目
	修改 promise文件夹里面的 index.js和databaseInitData里面数据库的地址

	命令行进入文件夹
	cd promise

	第一次启动时
	npm firstStart;

	平时启动
 	npm start;

 	promise主要代码在index里面 getBasicInfo请求函数里面




```
